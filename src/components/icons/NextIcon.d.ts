import type { IconProps } from "./LocationIcon";
interface NextIconProps extends IconProps {
  color?: string;
}
export declare function NextIcon({
  size,
  className,
  color,
}: NextIconProps): import("react/jsx-runtime").JSX.Element;
export {};
